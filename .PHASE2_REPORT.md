# FASE 2 - Limpieza de Datos (COMPLETADA)

**Date**: 2026-06-24  
**Status**: ✅ Completada

---

## Cambios Realizados

### 2.1 **Refactorización de `/sync/route.ts`**
- ❌ Eliminada: `external_calendar_connection.upsert()`
- ✅ Reemplazado: Ahora usa `oauthConnection.id` directamente de `ExternalConnection`
- ✅ Beneficio: Elimina redundancia, usa una sola tabla de conexiones

### 2.2 **Refactorización de `/disconnect/route.ts`**
- ❌ Eliminada: Actualización de `external_calendar_connection`
- ✅ Simplificada: Solo 3 operaciones (no 4)
- ✅ Beneficio: Transacción más limpia, menos deuda técnica

### 2.3 **Auditoría de Datos**
Creado: `/scripts/cleanup-phase2.ts` que audita:
- ❓ Purposes inválidos en `external_calendar` (no sean `crussader_mirror`/`google_context`)
- ❓ Appointments huérfanos (`externalProvider` set pero sin `externalCalendarId`)
- ❓ Registros no usados en `external_calendar_connection`

---

## Tabla `external_calendar_connection`: Decisión

**Estado**: 🟡 Deprecada pero NO eliminada aún

**Por qué**:
- Contiene datos históricos (audit trail)
- Ya no se escribe en ella (`/sync` y `/disconnect` refactorizados)
- Se leerá solo en endpoints obsoletos (`/import`, `/connected/[id]`)

**Plan**:
- FASE 2 ✅: Dejar de escribir en ella
- FASE 3: Eliminar endpoints que la usan (`/import`, `/connected/[id]`)
- FASE 3: Backup + eliminación segura

---

## Próximos Pasos (FASE 3)

### 3.1 **Ejecutar Auditoría**
```bash
tsx scripts/cleanup-phase2.ts
```
Esto mostrará números de:
- Purposes inválidos
- Appointments huérfanos
- Conexiones no usadas

### 3.2 **Decisiones Manuales**
Basado en output de auditoría:
- ¿Eliminar purposes inválidos o reclasificarlos?
- ¿Limpiar appointments huérfanos?
- ¿Backup antes de eliminar tabla?

### 3.3 **Eliminar Endpoints Obsoletos**
```
❌ /import
❌ /create
❌ /list
❌ /calendars
❌ /diagnose
❌ /connected/[id]
❌ /events/create
```

### 3.4 **Eliminar Tabla**
```sql
DROP TABLE external_calendar_connection CASCADE;
```

### 3.5 **Testing**
- Verificar que sync/disconnect/backfill funciona
- No debería haber cambios en comportamiento (solo limpieza interna)

---

## Garantías POST-FASE-2

✅ **Una sola table de conexiones**: `ExternalConnection` (no más duplicación)
✅ **Código más limpio**: Menos queries, menos transacciones complejas
✅ **Preparado para FASE 3**: Endpoints obsoletos listos para eliminación

---

## Archivos Modificados

```
✅ /app/api/integrations/google/calendar/sync/route.ts
   → Removido: external_calendar_connection.upsert()

✅ /app/api/integrations/google/calendar/disconnect/route.ts
   → Removido: external_calendar_connection.updateMany()

✨ /scripts/cleanup-phase2.ts
   → Nuevo: Auditoría de purposes inválidos, appointments huérfanos
```
