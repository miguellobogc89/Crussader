# VALIDACIÓN CAMPOS PRISMA - Google Calendar Endpoints

**Status**: ✅ TODOS LOS CAMPOS CORRECTOS

---

## 1. external_calendar (Tabla con connection_id)

**Schema**: 
```prisma
model external_calendar {
  connection_id        String
  company_id           String?
  location_id          String?
  external_calendar_id String
  name                 String
  timezone             String?
  purpose              String
  access_role          String?
  is_primary           Boolean
  is_app_created       Boolean
  active               Boolean
  created_at           DateTime
  updated_at           DateTime
  background_color     String?
  foreground_color     String?
  color_id             String?
}
```

**Campos CORRECTOS en código** (snake_case):
- ✅ `sync-calendars/route.ts:138` - `connection_id: connection.id`
- ✅ `sync-calendars/route.ts:122` - `company_id: companyId`
- ✅ `sync-calendars/route.ts:123` - `location_id: locationId`
- ✅ `sync-calendars/route.ts:142` - `external_calendar_id: calendar.id`
- ✅ `sync-calendars/route.ts:134` - `updated_at: new Date()`
- ✅ `callback/route.ts:228-232` - Todos los campos correctos

---

## 2. external_calendar_event (Tabla SIN connection_id después de delete)

**Schema** (después de SQL migration):
```prisma
model external_calendar_event {
  company_id           String
  location_id          String?
  provider             String
  external_calendar_id String
  external_event_id    String
  title                String?
  description          String?
  start_at             DateTime
  end_at               DateTime
  timezone             String?
  status               String?
  raw_payload          Json?
  created_at           DateTime
  updated_at           DateTime
}
```

**Campos CORRECTOS en código** (snake_case):
- ✅ `sync/route.ts:276` - `company_id: companyId` (update)
- ✅ `sync/route.ts:277` - `location_id: locationId` (update)
- ✅ `sync/route.ts:289` - `company_id: companyId` (create)
- ✅ `sync/route.ts:290` - `location_id: locationId` (create)
- ✅ `sync/route.ts:280` - `start_at: startAt` (update)
- ✅ `sync/route.ts:281` - `end_at: endAt` (update)
- ✅ `sync/route.ts:285` - `updated_at: new Date()` (update)
- ❌ NO hay `connection_id` en ningún lugar (CORRECTO - fue eliminado)

---

## 3. Appointment (Tabla con camelCase)

**Schema**:
```prisma
model Appointment {
  startAt              DateTime
  endAt                DateTime
  updatedAt            DateTime
  externalProvider     String?
  externalCalendarId   String?
  externalEventId      String?
  externalColor        String?
}
```

**Campos CORRECTOS en código** (camelCase):
- ✅ `sync/route.ts:328` - `startAt,` (update)
- ✅ `sync/route.ts:329` - `endAt,` (update)
- ✅ `sync/route.ts:331` - `updatedAt: new Date(),` (update)
- ✅ `sync/route.ts:337` - `startAt,` (create)
- ✅ `sync/route.ts:338` - `endAt,` (create)
- ✅ `backfill/route.ts:64` - `externalProvider: "google-calendar-pending"` (lock)
- ✅ `disconnect/route.ts:79` - `status: "CANCELLED"` (cancel)

---

## 4. RESUMEN DE VALIDACIÓN

| Tabla | Campos | Estatus | Archivos |
|-------|--------|---------|----------|
| external_calendar | ✅ snake_case | OK | sync-calendars, callback, sync, disconnect, connected |
| external_calendar_event | ✅ snake_case, NO connection_id | OK | sync, (import eliminado) |
| Appointment | ✅ camelCase | OK | sync, backfill, disconnect |

---

## 5. VERIFICACIÓN DE ELIMINACIÓN

**external_calendar_connection**:
- ✅ Eliminada de schema.prisma (post SQL migration)
- ✅ Eliminada de DB (ejecutar SQL migration en Neon)
- ✅ NO hay referencias en código google-calendar (búsqueda completa: 0 referencias)
- ✅ SOLO en documentación y script de auditoría

---

## 6. ✅ LISTO PARA BUILD

```bash
npx prisma generate     # ✅ PASS
npm run build          # SHOULD PASS después de migration
```

**Próxima acción**:
1. Ejecutar SQL en Neon (eliminar external_calendar_connection)
2. `npx prisma db pull`
3. Commit schema.prisma actualizado
4. `npm run build`

